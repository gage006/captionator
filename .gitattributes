# Normalize line endings to LF in the repo. Critical for files that run
# inside the Linux containers (shell scripts, Dockerfiles) — CRLF breaks them.
* text=auto eol=lf

*.png binary
*.jpg binary
*.mp4 binary
*.ico binary
